apiVersion: v1
kind: Service
metadata:
  name: final
spec:
  selector:
    app: final
#  type: NodePort
  ports:
  - protocol: TCP
    port: 80
#    nodePort: 32000
    targetPort: 8080
  externalIPs:
    - {{ NODE_IP }}
