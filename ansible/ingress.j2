apiVersion: networking.k8s.io/v1
kind: Ingress
metadata:
  name: final-ingress
  annotations:
    nginx.ingress.kubernetes.io/rewrite-target: /$1
spec:
  rules:
  - host: final.lo
    http:
      paths:
      - path: /
        backend:
          name: final
          port:
            number: 80